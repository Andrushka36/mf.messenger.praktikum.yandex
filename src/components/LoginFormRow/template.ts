export const template = `
    <label class="login-form__row">
        <input type="{{ type }}" class="login-form__input" value="{{ value }}" name="{{ name }}" required="required" />
        <span class="login-form__label">{{ label }}</span>
    </label>
`;
